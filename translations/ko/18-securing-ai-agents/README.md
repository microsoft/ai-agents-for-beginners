[강의 영상 보기: 암호화 영수증으로 AI 에이전트 보안 강화](https://youtu.be/PLACEHOLDER_VIDEO_ID)

> _(강의 영상과 썸네일은 병합 후 Microsoft 콘텐츠 팀에서 14 / 15강 패턴에 맞게 추가 예정입니다.)_

# 암호화 영수증으로 AI 에이전트 보안 강화

## 소개

이 강의에서는 다음 내용을 다룹니다:

- AI 에이전트를 위한 감사 추적이 컴플라이언스, 디버깅, 신뢰에 왜 중요한가.
- 암호화 영수증이란 무엇이며 서명되지 않은 로그 라인과 어떻게 다른가.
- 평범한 Python으로 에이전트의 도구 호출에 대한 서명된 영수증을 생성하는 방법.
- 오프라인에서 영수증을 검증하고 변조를 감지하는 방법.
- 영수증을 체인으로 연결하여 하나를 삭제하거나 순서를 바꾸면 체인이 깨지는 원리.
- 영수증이 증명하는 것과 명시적으로 증명하지 않는 것.

## 학습 목표

이 강의를 마치면 다음을 할 수 있게 됩니다:

- 에이전트 동작에 대한 암호학적 출처 증명이 필요한 실패 모드를 식별하기.
- 표준화된 JSON 페이로드에 대한 Ed25519 서명 영수증 생성하기.
- 서명자의 공개키만으로 독립적으로 영수증 검증하기.
- 수정된 영수증에 대해 검증을 다시 실행하여 변조 감지하기.
- 해시 체인 방식의 영수증 시퀀스를 구축하고 체인 구조가 중요한 이유 설명하기.
- 영수증이 증명하는 것(귀속, 무결성, 순서)과 증명하지 않는 것(행동의 정당성, 정책의 타당성)을 구분하기.

## 문제: 에이전트의 감사 추적

Contoso Travel을 위해 AI 에이전트를 배포했다고 가정합시다. 에이전트는 고객 요청을 읽고, 항공편 API를 호출해 옵션을 조회하며, 고객을 대신해 좌석을 예약합니다. 지난 분기 에이전트는 5만 건의 예약을 처리했습니다.

오늘 감사관이 방문했습니다. 간단한 질문을 던집니다: "에이전트가 무슨 일을 했는지 보여주세요."

당신은 로그 파일을 건넵니다. 감사관은 더 어려운 질문을 합니다: "이 로그가 편집되지 않았다는 걸 어떻게 알 수 있나요?"

이것이 감사 추적 문제입니다. 오늘날 대부분의 에이전트 배포는 다음에 의존합니다:

- **애플리케이션 로그**: 에이전트가 직접 작성하며 파일 시스템 접근 권한이 있는 누구나 수정 가능.
- **클라우드 로깅 서비스**: 플랫폼 수준에서 변조 증거 제공하지만 감사관이 플랫폼 운영자를 신뢰해야 함.
- **데이터베이스 트랜잭션 로그**: 데이터베이스 변경에 적합하지만 임의 도구 호출에는 부적합.

이들은 모두 감사관이 누군가(당신, 클라우드 공급자, 데이터베이스 벤더)를 신뢰하지 않고는 질문에 답할 수 없습니다. 내부 사용에는 신뢰가 허용되지만, 규제 대상 작업(금융, 의료, EU AI 법령 대상 등)에는 그렇지 않습니다.

암호화 영수증은 각 에이전트 동작을 독립적으로 검증 가능하게 만들어 이를 해결합니다. 감사관은 당신을 신뢰할 필요가 없고 공개키와 영수증만 있으면 됩니다.

## 암호화 영수증이란?

영수증은 에이전트가 수행한 작업을 기록하고 디지털 서명한 JSON 객체입니다.

```mermaid
flowchart LR
    A[에이전트가 도구를 실행함] --> B[영수증 페이로드 생성]
    B --> C[JSON RFC 8785 정규화]
    C --> E[정규화된 바이트 Ed25519 서명]
    E --> F[서명이 포함된 영수증]
    F --> G[감사자가 오프라인 검증]
    G --> H{서명 유효한가?}
    H -- yes --> I[변조 방지 증거]
    H -- no --> J[영수증 거부됨]
```

최소한의 영수증 예시는 다음과 같습니다:

```json
{
  "type": "agent.tool_call.v1",
  "agent_id": "contoso-travel-bot",
  "tool_name": "lookup_flights",
  "tool_args_hash": "sha256:a3f9c1...",
  "result_hash": "sha256:7b2e1d...",
  "policy_id": "contoso-travel-policy-v3",
  "timestamp": "2026-04-25T14:30:00Z",
  "sequence": 47,
  "previous_receipt_hash": "sha256:9d4e6a...",
  "signature": {
    "alg": "EdDSA",
    "sig": "c5af83...",
    "public_key": "8f3b2c..."
  }
}
```

이 세 가지 속성이 핵심 역할을 합니다:

1. <strong>서명</strong>. 영수증은 에이전트 게이트웨이가 Ed25519 비밀키로 서명합니다. 해당 공개키를 가진 누구나 오프라인에서 서명을 검증할 수 있습니다. 필드를 변조하면 서명이 무효화됩니다.

2. **정형 인코딩**. 서명 전에 영수증은 JSON Canonicalization Scheme (JCS, RFC 8785)을 사용해 직렬화됩니다. 이를 통해 동일한 논리적 영수증을 생산하는 두 구현이 바이트 단위로 동일한 출력을 생성합니다. 정형화가 없으면 서로 다른 JSON 직렬화기가 동일 내용에 대해 서로 다른 서명을 생성할 수 있습니다.

3. **해시 체이닝**. `previous_receipt_hash` 필드는 각 영수증을 이전 영수증과 연결합니다. 하나라도 삭제하거나 순서를 바꾸면 뒤에 오는 모든 영수증이 깨집니다. 개별 서명이 우회되어도 체인 수준에서 변조가 드러납니다.

이들 속성은 세 가지 보증을 제공합니다:

- <strong>귀속</strong>: 이 키가 이 콘텐츠에 서명했습니다.
- <strong>무결성</strong>: 서명 이후 콘텐츠가 변경되지 않았습니다.
- <strong>순서</strong>: 이 영수증은 체인에서 저 영수증 뒤에 생성되었습니다.

## 파이썬에서 영수증 생성하기

영수증 생성을 위해 특별한 라이브러리가 필요하지 않습니다. 암호화 기본구조는 널리 사용 가능하며 로직은 몇십 줄의 파이썬 코드입니다.

`code_samples/18-signed-receipts.ipynb` 연습 문제에서 전체 흐름을 단계별로 다룹니다. 요약 버전은 다음과 같습니다:

```python
import json
import hashlib
import base64
from nacl import signing
from jcs import canonicalize  # RFC 8785 정규 JSON

def b64url_nopad(data: bytes) -> str:
    return base64.urlsafe_b64encode(data).decode("ascii").rstrip("=")

def sha256_canonical(obj) -> str:
    """SHA-256 of a Python object's JCS-canonical JSON form."""
    return f"sha256:{hashlib.sha256(canonicalize(obj)).hexdigest()}"

# 서명 키 생성 또는 로드 (운영 환경에서는 키 볼트에 저장)
signing_key = signing.SigningKey.generate()
verify_key = signing_key.verify_key

# 영수증 페이로드 구성 (아직 서명 없음)
tool_args = {"origin": "SYD", "destination": "LAX"}
tool_result = [{"flight": "QF11", "price": 1850, "stops": 0}]

payload = {
    "type": "agent.tool_call.v1",
    "agent_id": "contoso-travel-bot",
    "tool_name": "lookup_flights",
    "tool_args_hash": sha256_canonical(tool_args),
    "result_hash": sha256_canonical(tool_result),
    "policy_id": "contoso-travel-policy-v3",
    "timestamp": "2026-04-25T14:30:00Z",
    "sequence": 0,
    "previous_receipt_hash": None,
}

# JCS 바이트를 직접 정규화하고 서명. PureEdDSA는 내부적으로 해시 처리함.
canonical_bytes = canonicalize(payload)
signature_bytes = signing_key.sign(canonical_bytes).signature

# 구조화된 서명 객체 첨부.
receipt = {
    **payload,
    "signature": {
        "alg": "EdDSA",
        "sig": b64url_nopad(signature_bytes),
        "public_key": b64url_nopad(bytes(verify_key)),
    },
}
```

이것이 전체 서명 파이프라인입니다. 노트북의 연습문제에서 각 단계를 자세히 확인할 수 있습니다.

## 영수증 검증 및 변조 감지

검증은 반대 동작입니다:

```python
import base64
import hashlib
from nacl import signing
from nacl.exceptions import BadSignatureError
from jcs import canonicalize

def b64url_decode(s: str) -> bytes:
    padding = "=" * ((4 - len(s) % 4) % 4)
    return base64.urlsafe_b64decode(s + padding)

def verify_receipt(receipt: dict) -> bool:
    # 서명은 구조화된 객체입니다: {"alg", "sig", "public_key"}.
    sig_obj = receipt.get("signature")
    if not sig_obj or sig_obj.get("alg") != "EdDSA":
        return False

    # 실제로 서명된 페이로드(서명을 제외한 모든 것)를 재구성합니다.
    payload = {k: v for k, v in receipt.items() if k != "signature"}

    canonical_bytes = canonicalize(payload)

    try:
        verify_key = signing.VerifyKey(b64url_decode(sig_obj["public_key"]))
        verify_key.verify(canonical_bytes, b64url_decode(sig_obj["sig"]))
        return True
    except BadSignatureError:
        return False
```

이 함수는 영수증을 입력 받아 서명이 유효하면 `True`를, 그렇지 않으면 `False`를 반환합니다. 네트워크 호출, 서비스 의존성 없이 제3자를 신뢰할 필요가 없습니다.

변조 감지를 직접 보기 위해, 노트북은 다음을 다룹니다:

1. 유효한 영수증을 생성하고 검증됨을 확인하기.
2. `tool_args_hash` 필드의 한 바이트를 변경하기.
3. 검증을 다시 실행하고 실패를 확인하기.

이는 영수증이 변조 증거임을 실습으로 보여줍니다: 아주 작은 수정이라도 서명을 깨뜨립니다.

## 다단계 에이전트를 위한 영수증 체인 구성

단일 서명 영수증은 한 행동만 보호합니다. 여러 영수증 체인은 동작 시퀀스를 보호합니다.

```mermaid
flowchart LR
    R0[영수증 0<br/>제네시스] --> R1[영수증 1]
    R1 --> R2[영수증 2]
    R2 --> R3[영수증 3]
    R1 -. previous_receipt_hash .-> R0
    R2 -. previous_receipt_hash .-> R1
    R3 -. previous_receipt_hash .-> R2
```

각 영수증은 이전 영수증의 해시를 기록합니다. 공격자가 영수증 2를 조용히 삭제하려면 다음 중 하나를 해야 합니다:

- 영수증 3의 `previous_receipt_hash` 필드를 변경 (영수증 3의 서명 무효화), 또는
- 수정된 영수증 3에 새 서명 위조 (에이전트 비밀키 필요).

비밀키가 하드웨어 키 볼트에 있고 공개키를 각 영수증과 함께 공개한다면 어느 공격도 탐지 없이 성공할 수 없습니다.

노트북은 다음을 살펴봅니다:

1. 3개의 영수증 체인 구축하기.
2. 각 영수증의 `previous_receipt_hash`가 앞 영수증의 실제 해시와 일치하는지 검증하기.
3. 중간 영수증 하나를 변조해 체인이 그 지점에서 끊어지는 것을 보기.

이렇게 하면 외부 감사관이 당신을 신뢰하지 않고도 검증할 수 있는 감사 추적이 완성됩니다.

## 영수증이 증명하는 것 (그리고 증명하지 않는 것)

이것은 이 강의에서 가장 중요한 부분입니다. 영수증은 강력하지만 한계가 있습니다.

**영수증은 세 가지를 증명합니다:**

1. <strong>귀속</strong>: 특정 키가 특정 페이로드에 서명했습니다.
2. <strong>무결성</strong>: 페이로드가 서명 이후 변경되지 않았습니다.
3. <strong>순서</strong>: 이 영수증이 체인에서 저 영수증 뒤에 위치합니다.

**영수증이 증명하지 않는 것:**

1. <strong>정확성</strong>: 에이전트 행동이 옳은 행동이었는지. 영수증은 정답이든 오답이든 서명할 수 있습니다.
2. **정책 준수**: `policy_id`에 명시된 정책이 정말로 평가되었거나, 평가되었더라면 행동을 허용했는지. 영수증은 주장된 바를 기록하지 실행 여부를 기록하지 않습니다.
3. **키 외 신원**: 영수증은 "이 키가 이 콘텐츠에 서명했다"고 말할 뿐, "이 사람이 승인했다"고 말하지 않습니다. 사람이나 조직과 키를 연결하려면 별도의 신원 인프라(디렉토리, 공개키 등록소 등)가 필요합니다.
4. **입력의 진실성**: 에이전트가 조작된 프롬프트를 받아 행동하면 영수증은 그 행동을 정확히 기록합니다. 영수증은 입력 검증의 상류가 아니라 하류에 있습니다.

이 경계가 중요한 이유는 두 가지입니다:

- 영수증이 어디에 유용한지 알려줍니다: 에이전트 행동을 감사 가능하고 변조 증거로 만들어 조직 경계를 넘어 신뢰성을 확보하는 것.
- 어떤 추가 계층이 필요한지 보여줍니다: 입력 검증(6강), 정책 집행(아래 간략히 언급), 신원 인프라(이 강의 범위 밖).

흔한 실수는 "우린 영수증이 있으니 '규제 받고 있다'라는 뜻"으로 오해하는 것입니다. 그렇지 않습니다. 영수증은 기반입니다. 규제는 그 위에 세우는 시스템입니다.

## 사람이 정확한 행동을 승인했음을 증명하기

앞의 3번 항목은 별도 섹션이 필요합니다: 행동 영수증은 "이 키가 이 콘텐츠에 서명했다"고 말할 뿐 "사람이 승인했다"고는 하지 않습니다. 환불, 삭제, 송금 같은 고위험 행동에서는 거버넌스 프레임워크가 점점 더 이 누락된 확인을 요구하며, 이미 이 강의에서 구현한 기본구조로도 생성 가능합니다.

후속 노트북 `code_samples/human-authorization-receipts.ipynb`는 두 번째 영수증 종류 `human.approval.v1`을 추가합니다. 이 영수증은 기존 강의의 영수증과 같은 형태로(정형화된 JCS 바이트에 Ed25519 서명, 서명 객체는 서명 바이트 외부에 위치) 동작합니다. 명명된 승인자가 실행 전에 <strong>전체 정형화 행동과 그 해시</strong>에 서명합니다; 에이전트의 행동 영수증은 동일한 행동 해시와 승인 `receipt_hash`를 참조하는 `parent_approval_ref`를 포함합니다. 하나의 `verify_chain`이 두 기록 모두를 서로 다른 고정 키 레지스트리 (승인자 키 대 에이전트 키) 하에서 검증하여 코드 경로는 공유하지만 권한은 분리합니다.

이 구조가 확보하는 바는 명확합니다: *사람이 이 정확한 행동을 승인했고, 에이전트는 승인된 정확한 행동만 실행했다.* 노트북 내 거부 테스트 케이스는 아래와 같은 점들을 명확히 합니다:

- 고전적인 문제들: 변조, 혼란된 대리자, 재생 공격, 양쪽에서의 서명 키 위조, 형식이 잘못된 입력;
- **만료된 권한**: 정책 버전 변경, 승인자 키의 고정 레지스트리 교체, 실행 전에 승인 만료 등으로 인해 서명이 검증되더라도 거부됨;
- **해시 대체**: 유효하게 서명된 행동 영수증이 전혀 다른 정형화 행동을 묶는 <em>실제</em> 승인을 가리킴.

각 실패는 명확한 이유로 거부되어 감사관이 권한이 만료됐는지, 실행 행동이 변경됐는지 판별할 수 있습니다. 노트북이 가르치는 규칙: 서명된 승인은 그 자체로 권한이 아닙니다. 실행 시점에 두 영수증이 모두 같은 정형화 행동에 묶여 있어야 권한이 있습니다. 사람 승인 영수증은 이 강의에서 정의한 교육용 조합이지 `draft-farley-acta-signed-receipts`가 정의한 영수증 유형은 아닙니다.

## 실무 참고자료

이 강의의 파이썬 코드는 각 줄을 읽고 무슨 일이 일어나는지 정확히 이해할 수 있도록 의도적으로 최소화되어 있습니다. 실무에서는 두 가지 선택지가 있습니다:

1. **암호화 기본구조 위에 직접 개발하기.** 위의 50줄 코드는 많은 사용 사례에 충분합니다. PyNaCl (Ed25519)과 `jcs` 패키지 (정형화 JSON)는 잘 유지보수되고 감사된 라이브러리입니다.

2. **실무용 영수증 라이브러리 사용하기.** 여러 오픈 소스 프로젝트가 같은 패턴을 추가 기능과 함께 구현합니다(키 교체, 일괄 검증, JWK 세트 배포, 정책 엔진 통합):
   - 서명 파이프라인은 독립적인 IETF 인터넷 초안([`draft-farley-acta-signed-receipts`](https://datatracker.ietf.org/doc/draft-farley-acta-signed-receipts/), 개정 02)에서 JCS와 서명 범위 규칙을 사용합니다. 이 강의의 평면 교육용 영수증은 초안의 `{payload, signature}` 형태와 다르며 호환 구현으로 제시되지 않습니다. 초안은 대상 와이어 포맷용으로 공유 준수 테스트 세트([agent-governance-testvectors](https://github.com/ScopeBlind/agent-governance-testvectors))를 공개합니다.
   - Microsoft Agent Governance Toolkit은 Cedar 기반 정책 결정과 영수증을 결합합니다; 해당 저장소의 튜토리얼 33에서 예제를 확인하세요.
   - `protect-mcp` (npm)와 `@veritasacta/verify` (npm) 패키지는 서버에 변조 증거 감사 추적 오프라인 검증을 적용하는 Node 기반 구현을 제공합니다. 멈춤 동작이 WebAuthn 지원 검증 흐름의 승인 영수증을 발급하는 공조 서명 흐름도 포함합니다.
   - **[nobulex](https://github.com/arian-gogani/nobulex)** 파이썬 SDK (`pip install nobulex`)는 LangChain 및 CrewAI 통합을 제공하는 동일한 Ed25519 + JCS 서명 패턴을 제공합니다. 크로스 검증 테스트 벡터 및 [OWASP PR #2210](https://github.com/OWASP/CheatSheetSeries/pull/2210)를 통한 규정 준수 매핑도 포함됩니다.

직접 구현할지 라이브러리를 사용할지는 JWT 라이브러리 선택과 비슷한 문제입니다: 둘 다 괜찮습니다; 라이브러리는 시간 절약과 감사 표면 감소를 제공하며, 직접 구현은 각 기본 구조를 완전히 이해하게 합니다. 이 강의는 어떤 선택지든 기초를 마련하도록 직접 구현 경로를 가르칩니다.

## 이해도 점검

실습 문제로 넘어가기 전에 이해도를 점검하세요.

**1. 영수증은 에이전트의 비밀 Ed25519 키로 서명됩니다. 감사관은 공개키만 가지고 있습니다. 감사관이 오프라인에서 영수증을 검증할 수 있나요?**

<details>
<summary>답변</summary>

예. Ed25519 검증은 공개키와 서명된 바이트만 필요합니다. 네트워크 호출 없이, 서비스 의존성 없이 가능합니다. 이것이 영수증이 단절된 네트워크, 다중 조직, 낮은 신뢰 감사 환경에서 유용한 이유입니다.
</details>

**2. 공격자가 영수증의 `policy_id` 필드를 더 관대한 정책으로 변경했다고 주장합니다. 서명은 원본 페이로드에 대해 이루어졌습니다. 검증 시 무슨 일이 발생할까요?**

<details>
<summary>답변</summary>


검증에 실패했습니다. 서명은 원본 페이로드의 정규화된 바이트를 기준으로 계산되었으며, 어떤 필드를 수정하면 해당 바이트가 변경되어 서명이 무효가 됩니다. 공격자는 새로 유효한 서명을 생성하려면 개인 키가 필요하지만, 이를 가지고 있지 않습니다.
</details>

**3. 왜 영수증에 원시 인수와 결과 대신 `tool_args_hash`와 `result_hash`가 포함되어 있습니까?**

<details>
<summary>답변</summary>

두 가지 이유가 있습니다. 첫째, 영수증은 원시 내용(개인 식별 정보, 비즈니스 데이터)이 노출되는 것이 문제인 환경에서 보관 또는 전송이 필요할 수 있습니다. 해싱은 영수증을 작게 유지하고 내용을 비공개로 유지하며, 감사자는 해시가 별도로 보관된 실제 내용과 일치하는지 확인합니다. 둘째, 해시는 고정 크기를 가지므로, 입력과 출력이 크더라도 해시를 포함하는 영수증은 크기가 제한됩니다.
</details>

**4. `previous_receipt_hash` 필드는 각 영수증을 이전 영수증과 연결합니다. 공격자가 체인 중간의 한 영수증을 몰래 삭제하면 무엇이 무효가 됩니까?**

<details>
<summary>답변</summary>

삭제된 영수증 이후의 모든 영수증이 무효가 됩니다. 이들의 `previous_receipt_hash` 필드는 더 이상 실제 체인과 일치하지 않기 때문입니다(참조된 영수증이 존재하지 않거나 체인이 다른 선행 영수증을 가리키게 됨). 삭제를 숨기려면 공격자는 이후 모든 영수증을 다시 서명해야 하는데, 이는 개인 키가 있어야 가능합니다.
</details>

**5. 영수증이 명확히 검증됩니다. 이것이 에이전트의 행동이 올바르거나 건전하며 정책을 준수했다는 증거가 됩니까?**

<details>
<summary>답변</summary>

아닙니다. 유효한 영수증은 세 가지를 증명합니다: 귀속 (이 키가 이 콘텐츠에 서명함), 무결성 (내용이 변경되지 않음), 순서 (이 영수증이 그 영수증 이후에 옴). 에이전트 행동이 올바르다거나 `policy_id`에 명시된 정책이 실제로 평가되었거나 에이전트가 모든 규칙을 따랐다는 것을 증명하지 않습니다. 영수증은 에이전트 행동을 감사 가능하게 만들지만, 반드시 올바르게 만들지는 않습니다. 이것이 이 수업에서 가장 중요한 한계입니다.
</details>

## 실습 연습문제

`code_samples/18-signed-receipts.ipynb`을 열고 네 섹션을 모두 완료하세요:

1. **섹션 1**: 첫 번째 영수증에 서명하고 검증하세요.
2. **섹션 2**: 영수증을 변조하고 검증 실패를 관찰하세요.
3. **섹션 3**: 세 개 영수증 체인을 만들고 체인 무결성을 검증하세요.
4. **섹션 4**: Microsoft Agent Framework로 구축한 에이전트에 이 패턴을 적용하세요: 툴 호출을 영수증 서명으로 감싸고, 독립적으로 영수증을 검증하세요.

**확장 도전 과제 1:** 직접 선택한 추가 필드(예: 추적용 요청 ID)를 영수증 스키마에 추가하고, 정규 서명 로직에 포함시킨 뒤, 영수증이 여전히 검증되도록 확인하세요. 그런 다음 서명 후 필드를 수정하고 검증 실패를 확인하세요. 이는 정규 인코딩의 모든 바이트가 서명에 어떻게 기여하는지 이해하게 합니다.

**확장 도전 과제 2:** 두 영수증의 해시를 SHA-256으로 함께 해싱하고(정규 바이트를 결정적 순서로 연결) 결과 다이제스트를 세 번째 영수증의 새 필드에 삽입한 뒤 서명하세요. 세 영수증이 여전히 검증되는지 확인하세요. 이는 한 단계 포함 증명을 만드는 것으로, 세 번째 영수증을 가진 누구나 첫 번째와 두 번째 영수증이 서명 시점에 존재했음을 증명할 수 있으며, 내용 공개는 필요 없습니다. 이는 선별적 공개 영수증에서 대규모로 사용하는 패턴입니다 (머클 커밋먼트, RFC 6962).

## 결론

암호학적 영수증은 AI 에이전트에게 다음과 같은 감사 추적을 제공합니다:

- **독립적 검증 가능**: 공개 키를 가진 누구나 검증 가능하며, 서비스 의존성 없음.
- **변조 탐지 가능**: 어느 부분이라도 수정 시 서명 무효.
- **이식 가능**: 영수증은 작은 JSON 파일로, 보관, 전송, 어디서나 검증 가능.
- **표준 준수**: Ed25519 (RFC 8032), JCS (RFC 8785), SHA-256 위에 구축, 널리 배포된 원시 암호학.

이들은 입력 검증, 정책 집행, 신원 인프라를 대체하지 않습니다. 이 계층들의 기반을 제공합니다. 에이전트를 규제된 작업, 다중 조직 워크플로우, 미래 감사자가 신뢰할 수 없을 환경에 배포할 때, 영수증은 감사 추적을 정직하게 만드는 방법입니다.

가장 중요한 요점: 영수증은 누가 언제 무엇을 말했는지를 증명합니다. 그것이 사실이거나 옳았음을 증명하지는 않습니다. 이 구분을 확실히 하세요. 이것이 정직한 출처 시스템과 오해를 일으키는 시스템의 차이입니다.

## 운영 준비 점검표

이 수업을 마치고 실제 환경에서 영수증 서명 에이전트를 배포할 준비가 되었을 때:

- [ ] **서명 키를 개발자 노트북에서 분리하세요.** Azure Key Vault, AWS KMS, 하드웨어 보안 모듈을 사용하세요. 영수증 서명용 개인 키는 소스 제어나 애플리케이션 기계의 평문에 절대 저장되면 안 됩니다.
- [ ] **검증용 공개 키를 공개하세요.** 감사자가 오프라인에서 검증할 수 있게 해야 합니다. 표준 패턴은 JWK 세트를 잘 알려진 URL (RFC 7517)에 공개하는 것입니다. 예: `https://your-org.example.com/.well-known/agent-keys.json`.
- [ ] **체인을 외부에 연계하세요.** 주기적으로 최신 체인 헤드 해시를 투명성 로그(Sigstore Rekor, RFC 3161 타임스탬프 권한, 또는 두 번째 내부 시스템)에 기록하여 외부 당사자가 "이 체인이 이 시점에 존재했다"는 것을 확인하게 하세요.
- [ ] **영수증을 불변으로 보관하세요.** 추가 전용 블롭 저장소(Azure Storage 불변 정책, AWS S3 Object Lock)가 저장 계층에서 내부자가 기록을 다시 쓰는 것을 방지합니다.
- [ ] **보관 정책을 결정하세요.** 다수의 컴플라이언스 체계에서 다년간 보관을 요구합니다. 영수증 증가 계획을 세우세요(영수증 하나당 약 500 바이트, 매일 1만 호출하는 에이전트는 연간 약 1.8GB 생성).
- [ ] **영수증이 다루지 않는 부분을 문서화하세요.** 영수증은 귀속, 무결성, 순서만 증명합니다. 실행 절차서에 입력 검증, 정책 집행, 속도 제한, 신원 인프라 등 영수증과 함께 작동하는 추가 제어책을 명확히 나열하세요.

### AI 에이전트 보안에 대해 더 궁금하신가요?

[Microsoft Foundry Discord](https://aka.ms/ai-agents/discord)에 참여하여 다른 학습자와 만나고, 오피스 아워에 참석하고, AI 에이전트 관련 질문에 답하세요.

## 이 수업 이후

이 수업은 단일 영수증 서명과 해시 체인 시퀀스를 다룹니다. 동일한 원시 암호학은 거버넌스 자세가 성숙해질 때 흔히 접하는 몇 가지 더 고급 패턴으로 조합됩니다:

- **선별적 공개.** 영수증 필드가 개별로 커밋될 때(RFC 6962 스타일 머클 트리), 특정 필드를 특정 감사자에게 공개하고 나머지는 변경되지 않았음을 증명할 수 있습니다. 동일 영수증이 포괄적 감사(완전성 원함)와 데이터 최소화 규제(GDPR, 최소한만 감사자에게 제공)를 모두 만족시켜야 할 때 유용합니다.
- **영수증 취소.** 서명 키가 노출되면, 해당 키가 서명한 모든 영수증을 해당 시점 이후로 신뢰할 수 없다고 표시할 방법이 필요합니다. 표준 패턴: 단기 서명 키+공개 취소 목록 또는 취소 항목이 포함된 투명성 로그.
- **양방향 / 분할 서명 영수증.** 일부 구현은 서명된 페이로드를 실행 전(`authorization_*`)과 실행 후(`result_*`) 절반으로 분리하여 독립 서명을 적용합니다. 인가 결정과 관찰된 결과가 다른 행위자나 시점에 의해 생성될 때 유용합니다. 이 패턴은 이 수업에서 가르친 영수증 포맷 위에 덧붙일 수 있습니다.
- **페이로드 구성.** 영수증은 `result_hash`에 넣은 바이트를 봉인합니다. 실제 페이로드는 단일 툴 호출 결과보다 풍부합니다: 예비 의사결정 근거(모델 예측, 고려 옵션, 근거와 그 완전성, 위험 자세, 책임 체인, 관문 결과)가 모두 페이로드 내에 포함될 수 있으며, 단일 영수증으로 봉인될 수 있습니다. 영수증 포맷을 최소화하면서 도메인별 페이로드 스키마 발전을 허용합니다.
- **다중 구현 호환성.** 동일 영수증 포맷의 파이썬, 타입스크립트, 러스트, 고 구현은 공유 테스트 벡터로 상호 검증합니다. 직접 구현한다면 공개된 벡터로 검증하여 통신 호환성을 확인합니다.
- **포스트 양자 이행.** Ed25519는 현재 널리 쓰이지만 양자 저항성은 없습니다. 영수증 포맷은 알고리즘 변경이 가능하며, `signature.alg` 필드는 필요할 때 NIST 포스트 양자 서명 표준인 `ML-DSA-65`를 담을 수 있습니다. 이행 기간 동안 영수증을 이중 서명하는 계획을 세우세요.

## 참고 자료

- <a href="https://datatracker.ietf.org/doc/draft-farley-acta-signed-receipts/" target="_blank">IETF 인터넷 초안: 기계 간 접근 통제용 서명된 결정 영수증</a>
- <a href="https://learn.microsoft.com/azure/ai-studio/responsible-use-of-ai-overview" target="_blank">책임 있는 AI 개요 (Azure AI)</a>
- <a href="https://datatracker.ietf.org/doc/html/rfc8032" target="_blank">RFC 8032: 에드워즈-곡선 디지털 서명 알고리즘 (EdDSA)</a>
- <a href="https://datatracker.ietf.org/doc/html/rfc8785" target="_blank">RFC 8785: JSON 정규화 방식 (JCS)</a>
- <a href="https://datatracker.ietf.org/doc/html/rfc6962" target="_blank">RFC 6962: 인증서 투명성</a> (선별적 공개 영수증에 사용되는 머클 트리 구성)
- <a href="https://github.com/microsoft/agent-governance-toolkit/blob/main/docs/tutorials/33-offline-verifiable-receipts.md" target="_blank">Microsoft Agent Governance Toolkit, 튜토리얼 33: 오프라인 검증 가능한 결정 영수증</a>
- <a href="https://github.com/ScopeBlind/agent-governance-testvectors" target="_blank">이 수업에서 사용하는 영수증 포맷의 다중 구현 호환성 테스트 벡터 (Apache-2.0)</a>
- <a href="https://pynacl.readthedocs.io/" target="_blank">PyNaCl 문서</a> (파이썬 Ed25519)

## 이전 수업

[로컬 AI 에이전트 생성하기](../17-creating-local-ai-agents/README.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**면책 조항**:
이 문서는 AI 번역 서비스 [Co-op Translator](https://github.com/Azure/co-op-translator)를 사용하여 번역되었습니다. 정확성을 기하기 위해 노력하고 있으나, 자동 번역은 오류나 부정확한 부분이 있을 수 있음을 유의하시기 바랍니다. 원본 문서의 원어본이 권위 있는 자료로 간주되어야 합니다. 중요한 정보의 경우, 전문가의 인간 번역을 권장합니다. 이 번역 사용으로 인해 발생하는 오해나 잘못된 해석에 대해 당사는 책임을 지지 않습니다.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->